# Copier en .env.local pour le dev, ou définir comme "Environment Variable"
# dans Render (Static Site) au moment du build.

# URL complète de l'API backend déployée, avec le préfixe /api/v1
VITE_API_URL=https://<votre-backend>.onrender.com/api/v1
